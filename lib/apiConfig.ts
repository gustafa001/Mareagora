/**
 * Os dados de maré agora são JSONs estáticos em /data/
 * servidos diretamente pelo Vercel — sem servidor externo.
 * Esta constante existe apenas para compatibilidade.
 */
export const API_BASE = '';
